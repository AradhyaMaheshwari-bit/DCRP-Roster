# DCRP-Roster — runtime dependencies
# Pinned to a range that gives stability without locking patch versions.

# Discord client
discord.py>=2.3,<3

# Google Sheets
gspread>=6.1,<7
google-auth>=2.27,<3

# Config / data
pydantic-settings>=2.2,<3
PyYAML>=6.0,<7
python-dateutil>=2.9,<3

# --- dev / test ---
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
